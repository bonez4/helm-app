# PII-bearing extract artifacts from master_list_extract.py — generated locally,
# never committed to the public Pages repo.
master_extract.json
master_extract_*.csv
dryrun_*.csv
dryrun_summary.txt

# Audit artifacts from helm_delta_audit.py (acct lists with names, addrs, phones).
audit_*.csv
audit_summary.txt
HELM_Delta_Audit.xlsx

# Import dry-run + applied logs from import_rates.py / import_routes.py.
import_rates_preview.csv
import_rates_applied.csv
import_routes_preview.csv
import_routes_applied.csv

# find_missing_accts.py output — PII (acct + name + address)
missing_from_helm.csv

# bulk_import_missing_accts.py outputs — PII
bulk_import_preview.csv
bulk_import_applied.csv

# Day-route master list PDFs themselves carry customer names + addresses
master list pdfs/

# Ad-hoc Python helpers used during PDF exploration (kept locally only).
peek_*.py

# import_route_notes_xlsx.py outputs (PII)
route_notes_preview.csv
route_notes_skipped.csv
route_notes_applied_log.csv
peek_stops.py

# sync_client_route_fallback.py output (PII)
sync_route_fallback_preview.csv

# Source scale export (PII — customer-by-customer ticket history)
1776188206521-*.xls

# min_tonnage_analysis.py output (PII — per-ticket customer + revenue)
Tonnage_Analysis_*.xlsx

# Python bytecode cache
__pycache__/

# Delta export PDFs (PII — customer names + addresses). README documents these as local-only.
delta export*.pdf

# All Excel workbooks are PII-bearing data extracts/analyses — keep local-only.
# client_import_template.xlsx is the one intentionally-tracked template (re-included below).
*.xlsx
!client_import_template.xlsx

# Local agent/editor settings + worktrees
.claude/

# Stray phone photo dropped into repo root (PII / not an app asset)
IMG_9452.png
